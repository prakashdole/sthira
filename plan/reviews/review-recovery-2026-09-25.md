# Recovery acceptance review — 2026-09-25

Reviewed `CLEAN` at `e70f512`, changes `b48a372..e70f512` (11 commits). This is a bounded integration review of C00–C05 and their affected callers, not a fresh audit of every historical line. Code-review graph incrementally updated to HEAD; direct source checks supplied graph gaps. No application fixes, deployment, weight download or cloud access in this review.

## Verdict

**Demo engineering is not accepted.** Real instance/model evaluation is additional work, not the only remaining work. Preserve C01 authority/candidate corrections, C03 DB tuple/conflict checks, C04 shared pipeline composition, C05 live schema query and the removal of drop-first DB setup. Reopen the specific C02–C05 requirements below. C06–C09 remain unfinished under their existing scheduling/authorization gates; no new phase or architecture is proposed.

## Findings and reproduction evidence

| ID | Severity / owner | Finding and bounded evidence |
| --- | --- | --- |
| F01 | High / C02 | `mapActions.ts:validateVoiceResponse` requires schema `1.0`; actual Go `/api/v3/voice/process` returns `3.0`. Captured a real HTTP+DB proposal from isolated fake workers, passed it to the shipped helper: rejected. Changing only schema to `1.0` accepted. Browser ignores `executeMapActions`'s false result. Unit map tests do not cover the actual Go response. |
| F02 | High / C02+C04 | Browser records `audio/webm;codecs=opus`; public `IsSupportedTranscriptionContentType` accepts only bare types. Private `DecodeAudio` also rejects that exact browser string (it lists a different spacing variant). Temporary decoder probe failed with `unsupported audio codec: audio/webm;codecs=opus` before decoding. No microphone/model execution claimed. Fix MIME parameter handling and test actual recordings, not empty-WAV fake workers. |
| F03 | High / C02 | Real browser Start route created a RESERVED stay. Reload showed Start safe route again because `reconcileStayState` expects `CONFIRMED`; server returns `RESERVED`. Another click created a second RESERVED stay for the same browser session (DB count 1→2). Existing active ID is overwritten; old reservation still holds capacity. Pending retries preserve only key, reconstructing payload/dates/target. |
| F04 | High / C02 | Quarantined only the owned exercise source. Actual guidance API returned `source_status=UNAVAILABLE`, no destinations. Reloaded browser removed destination but source details displayed CURRENT, while still displaying “Use the approved Ridge Road route now”, directions and Listen. UI reads nonexistent `metadata.freshness` with CURRENT default. Online handler does not refresh guidance or reconcile state. Static route/ETA/freshness claims and fallback route IDs remain. |
| F05 | High / C02 | Source inspection: hidden-tab handler only sets flags; never clears geolocation watch. The callback suppresses display while hidden, which does not stop capture. Operator navigation also does not stop capture. Recorder setup lacks complete track/timer cleanup on failure/cancellation. No permission to collect real user location/audio was exercised. |
| F06 | High / C02 | Source inspection: local speech-key prose differs from approved templates, ignores args/version, and unknown keys become “Official guidance: [key]”. Replay passes only base64; hash/size/type checks are optional, size tolerates four bytes, settings are ignored. Listen/fallback still uses browser speech synthesis. Selected TTS output must not be replaced or integrity checks skipped. |
| F07 | Medium / C03 | Temporary helper probe: approved EN+ML languages plus a flat `welcome` digest, with no `welcome/ml-IN`, returns that flat digest as accepted for ML. Runtime fallback was retained for old tests despite C03 forbidding global fallback. Current DB resolver emits tuples; this probe does NOT demonstrate a bypass of that resolver or of snapshot revalidation. Remove compatibility fallback and migrate tests. |
| F08 | High / C04 | `execution-c04.md` startup commands do not match implementation. Speech Python adapters are JSON-line stdin/stdout runtimes, not HTTP services; raw vLLM is not the typed middle worker. Package/entry-point inventory found only worker libraries/tests/evaluators, not real ASR/TTS/middle service launch binaries. Shared API wiring accepts supplied URLs but does not compose those services. Reuse existing libraries for minimal executable entry points; then verify missing-config/artifact failure and actual models separately. |
| F09 | High / C05 | Port collision reproduced during first DB suite while this review's owned server occupied 8080. Runner's own child could not bind; readiness accepted the other server. Runner created and ARRIVED a reservation in the other owned DB, then failed when stopping its own mock workers did not stop the other server's voice pipeline (HTTP 200 instead of 503). Both environments were disposable. This is a runner ownership defect, not a failure of reservation transactions. Suite passed after the review listener stopped; that does not close collision handling. |
| F10 | High / C05 | Source inspection: `--serve` kills workers for outage, restarts at a fresh ephemeral port, but leaves backend on old URLs; Vite target remains hardcoded 8080 even if runner port changes. Reuse mode seeds/migrates an existing DB and must not be inferred safe. Database name sanitization silently changes names and lacks the 63-byte limit. Prove ownership, cleanup and final usable served state rather than accepting startup logs. |
| F11 | Medium / C05+C00 | Rehearsal claims exceed checks: Node proximity units are not UI GPS lifecycle; a second successful same-key call is replay, not a dropped response; stopping workers is not offline reconnect; `docker info` is not container deployment/persistence/restore. Readiness counts any OPERATIONAL source (including synthetic seed) and model `Ready` without `Warm`; handler presence alone is not healthy inference. Keep API readiness separate and narrow each reported claim. `round-two-demo.md` still claimed engineering acceptance. |

## Verification actually run

- All seven Go modules' standard suites passed. Separate modules: backend, asrworker, ttsworker, middleworker, backend/eval, loadmodel, deploy/go-backend/migrate. Some unchanged results were cached; do not call these new GPU or deployment measurements.
- Backend rerun `go test -json -count=1 ./...` on an owned PostgreSQL database migrated through 0010: exit 0, 926 test/subtest RUN events, 942 PASS events including package completions; no named test failure/skip. Five packages emitted no-test package skip events. Counts are not independent product requirements.
- First DB suite failed `TestC05_FullIntegratedRehearsalScript` during the controlled local-port overlap described in F09; its later unoccupied-port pass does not erase that finding.
- Frontend: 43 unit tests passed; TypeScript/Vite production build passed, with an existing large-chunk warning. No new C02 served-flow regression tests were added in the reviewed commit window.
- Actual browser: local exercise page with Go+PostgreSQL and explicitly fake protocol workers; route reservation, reload, duplicate reservation, quarantine/reload and source-details read. Actual HTTP proposal fed into shipped map validator. No actual inference/real microphone/GPS/phone-device claim.
- Temporary failing language-digest and MIME decoder probes were removed immediately. Both failed for the expected requirement violation; no product source changes retained.
- Both real process tests executed and passed with `-tags crashtest` and `STHIRA_RUN_PROCESS_TESTS=1`: `TestCrossProcessLastSpace` and `TestCrashAfterCommitBeforeResponse`. Backend `go vet ./...` and `go build ./...` passed.

Owned review services/browser were stopped and the disposable database, temporary binaries, probe files and raw DB test logs removed. Shared databases, existing worktrees and user instances were untouched.

## Execution and remaining gates

Use `plan/prompt.md` section **0.0**, which contains the ordered repair instructions, ownership and regression requirements. Do C05 ownership safety before reusing the runner; C03/C04 service/codec work and C02 UI work can run in two explicitly assigned isolated lanes. Final browser rehearsal is serial after integration. No new parallel agents are authorized by this report alone.

Real model acceptance: run selected ASR → Sarvam → approved text/TTS → browser actions/audio on authorized instances, with artifact/runtime identity, actual language utterances, latency/failure observations and no stubs. Keep private worker services behind the backend; no instance credentials in Git. Provisioning alone does not establish inference, load, security or budget acceptance.

Later work: C06 native build/offline correctness; C07 native API journeys; C08 real device/language/system acceptance; C09 bounded served-security/helper disposition. O01/O05/O06/O07/O08/O11/O14 and relevant device/privacy/distribution decisions stay open. Do not call government APIs the only remaining production dependency.
