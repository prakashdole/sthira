# Decision record

2026-09-19. **Accepted** means explicitly chosen by the user or retained as a governing safety boundary. **Proposed** means an implementation recommendation awaiting its stated gate. Superseded details remain in Git rather than the active backlog.

| ID | Status | Decision and reason | Revisit / evidence |
| --- | --- | --- | --- |
| D01 | Accepted | Immediate evacuation and 7–30 day temporary stays; no permanent relocation or safe-land/hazard prediction | User pivot, 2026-09-19 |
| D02 | Accepted | Go owns product backend; migrate required behavior rather than every legacy module | User pivot; P0 inventory |
| D03 | Accepted | Android and iPhone available from launch; defer frontend framework until backend handoff P7 | User clarification |
| D04 | Accepted | One million total users; benchmark explicit incident concurrency scenarios | User clarification; sizing assumptions P7 |
| D05 | Accepted | 10–15 demo states, 2–3 flood/landslide cases per state, regional languages; user supplies zones later | State list proposed in feature.md |
| D06 | Accepted | ASR → middle language model → TTS with voice-first UX and chat/touch alternatives | User pivot; no unnecessary spoken acknowledgments |
| D07 | Accepted | Government owns operational zones, facilities, capacity and instructions. Model cannot generate operational facts | User pivot and retained authority boundary |
| D08 | Open | Route approval/verification ownership is not yet selected | User explicitly left open; O05 |
| D09 | Accepted | User chooses among eligible destinations and route options where policy allows | User's local-knowledge requirement; no unverified route endorsement |
| D10 | Accepted | Local UI/assets/maps/cache; server AI and authoritative dynamic data | User requirement; offline freshness still enforced |
| D11 | Proposed | Use isolated established Python/native inference runtimes while product logic moves to Go | Avoid porting vLLM/ASR/TTS frameworks; P6 compatibility record |
| D12 | Proposed | Modular Go service, PostgreSQL/PostGIS, SQL transactions, HTTP/CDN first; no compulsory broker/Redis/Kubernetes | Smallest operational architecture; expand only on evidence |
| D13 | Superseded by D59 | Benchmark Qwen3-4B-Instruct-2507 first with private vLLM; final model selected on regional intent/entity performance | P6 corpus, license/version/hardware evaluation; SUPERSEDED by D59 (user explicit selection of Sarvam-30B FP8) |
| D14 | Proposed | Retain IndicConformer and Indic Parler-TTS as first candidates, not blanket verified language support | P6 exact artifact/license and per-language evidence |
| D59 | Accepted | User model stack selection: Middle model adopted as Sarvam-30B MoE (sarvamai/sarvam-30b, Apache-2.0, 2.4B active non-embedding params out of 30B total, FP8 quantization); ASR pinned as AI4Bharat IndicConformer-600M-Multi; TTS pinned as AI4Bharat Indic Parler-TTS. Supersedes earlier Qwen-first/~5–6B ceiling preference. Capacity sizing accounts for 30B MoE total weights (~30 GB in FP8) rather than 2.4B active parameter memory. Non-thinking / disabled reasoning enforced so thinking tokens cannot leak into JSON action channel. Real inference remains evaluation-gated pending designated GPU hardware and language review evidence (O03/O11) | User explicit instruction 2026-09-21; sarvamai/sarvam-30b model card verification |
| D15 | Proposed | MapLibre Native with licensed downloadable vector regions; remove dependence on raster imagery | Both-platform offline/device tests P8; map license O06 |
| D16 | Retained | Explicit arrival and dialler confirmation; no geofence, automatic calling or model capacity writes | Existing safety boundary; conservation tests |
| D17 | Retained | Durable atomic idempotent capacity, authoritative versions and invalidation | P3/P4/P7 database/failure evidence |
| D18 | Accepted | This task changes plan Markdown only. Actual cleanup and Go implementation are later phases | User's final scope/cost instruction |
| D19 | Proposed | Layered OSS assurance: Go native checks, Staticcheck/pprof/k6, vulnerability/secret scans, ZAP plus scoped Strix | P7/P9; Strix is not a security certificate |
| D20 | Accepted | Frontend starts at measurable backend gate B, not a claimed 80–90% based on LOC | Operational definition of requested sequence |
| D21 | Accepted | Go 1.27.1 pinned as the backend toolchain; stdlib-only P1 slice builds offline | P1; revisit on supported-release cadence |
| D22 | Accepted | `/api/v3` contract slice frozen in `backend/contracts/openapi.yaml`; v2 kept only as reference for the old demo client during migration | P1; P10 retires v2 compatibility |
| D23 | Accepted | Strict JSON boundary rejects duplicate keys, unknown fields, trailing data, oversized bodies and excess depth before typed decoding | P1; R05 trust boundary |
| D24 | Accepted | Citizen sessions are account-free capability tokens: crypto/rand bearer, only SHA-256 hash stored, expiry + revocation; knowing an ID is not authorization (R22) | P4; no mandatory civil identity |
| D25 | Superseded | Operator authority requires verified identity + MFA (sessions.mfa_verified_at), not the OPERATOR label alone; operations jurisdiction-scoped, cross-jurisdiction FORBIDDEN | P4; R22; SUPERSEDED by D29 — the original slice trusted a caller-supplied request-body `mfa_verified:true` flag and caller-chosen jurisdiction, which is self-attestation, not verified identity or MFA |
| D29 | Accepted | Operator issuance requires a server-verified identity + MFA from a trusted authentication boundary (OperatorVerifier seam); jurisdiction/privilege derive from server-controlled operator_grants bound to the verified subject, never from the request; fails closed (503) with no verifier wired; synthetic verifier only in isolated tests, never in the production binary. Live operator IdP is BLOCKED_EXTERNAL (no IdP in deps) — recorded in open-decisions, NOT deferred to P11 | P4; R22; corrects D25 |
| D30 | Accepted | Operator idempotency is resource-bound: the target ID is folded into the operation name (source.transition:<id>, source.quarantine:<id>, stay.correction:<id>) over the existing IdempotencyStore, and current authorization for the target is revalidated before any replay result is disclosed; same-key/different-target is a distinct resource-scoped key, never success for the wrong target. Compatibility: the unique key is (scope, operation, idem_key); pre-change completed rows used bare operation names, so they occupy a different key space and are never wrongly replayed under the new target-scoped names — old rows are inert, no migration needed | P4; idempotency integrity |
| D31 | Accepted | Quarantine is a terminal sourceact QUARANTINED state distinct from SUSPENDED, reachable from all active states; quarantined evidence is excluded from the resolved operational context and new-reservation revalidation; existing stays are not cancelled nor capacity released | P4; source lifecycle |
| D32 | Accepted | The voice-commands context resolver is persisted: store.ResolveContext/ResolveAnyOperationalContext derive data version, known IDs and languages from one consistent current OPERATIONAL+authorized package row, failing closed on absent/expired/unauthorized/quarantined evidence; the static demo resolver stays explicitly non-operational | P4; no client-echoed facts |
| D26 | Accepted | Stay dates are half-open [start, end) in facility-local-date semantics (facilities.timezone); capacity E = free+held+occupied conserved per service_date; arrival converts held->occupied with no second decrement | P4; equations.md |
| D27 | Accepted | Operator stay correction is downward-only (releases space, always conservation-safe); an increase needs new capacity and is rejected rather than silently overbooking | P4; capacity conservation |
| D28 | Accepted | Multi-date reservations/transfers lock inventory in deterministic (facility_id, service_date) sorted order to avoid deadlock | P4; concurrency evidence |
| D33 | Accepted | Operator sessions persist the verified subject + authorizing grant (sessions.operator_subject/operator_grant_id, migration 0005); every protected operation revalidates the CURRENT grant via a FOR UPDATE row lock before any replay disclosure or mutation, so a withdrawn/expired grant denies both and cannot be bypassed by a stale check; legacy unbound (self-attested) operator sessions are revoked by the migration, never inferred | P4; R22; corrects the gap D29 left (issuance-only binding) |
| D34 | Accepted | Reservation commit revalidates the full operational context in-transaction: source OPERATIONAL + live jurisdiction authorization + package effective/unexpired/not-superseded (FOR UPDATE, serializing against concurrent quarantine/revocation/supersession) + facility-in-package + route verified/valid/unclosed; quarantine/suspend/revoke/supersede deny a NEW reservation with capacity unchanged, while an already-committed reservation's replay still returns its stored result | P4; eligibility at commit |
| D35 | Accepted | Reservation hold expiry derives from the authoritative allocation_policy + server time and is persisted atomically on reservation + stay; missing/invalid policy is rejected (no invented defaults); out-of-policy dates/extensions can't allocate; transfer writes a FRESH policy-derived deadline, never the old stay's due one | P4; expiry/policy wiring |
| D36 | Accepted | Voice context resolution is scoped to the requested jurisdiction (untrusted lookup input on VoiceCommandRequest.jurisdiction); the persisted resolver uses store.ResolveContext (jurisdiction-scoped), never the highest-version package across jurisdictions; missing jurisdiction 400, unknown 503, no substitution, cross-jurisdiction IDs rejected | P4; no client-echoed facts; corrects D32's global-selection gap |
| D37 | Accepted | Route validation at commit and at transfer binds the route to the selected facility's safe zone: route_versions.to_safe_zone_id must equal facilities.safe_zone_id for the same package. A route to a different safe zone (including the OLD facility's route reused at TRANSFER) is rejected with ErrRouteUnavailable; capacity unchanged | P4; route-facility binding; prevents transferring a stay with the origin's route |
| D38 | Accepted | The synthetic route gate is reachable ONLY via the store-level ChoiceQuery.RouteGateOpen field. The HTTP guidance/query handler hardcodes RouteGateOpen=false; the X-Sthira-Synthetic-Route-Gate request header is ignored (no header control). O05 fail-closed boundary is enforced at the API boundary | P4; isolates synthetic routes from public requests; corrects D34's missing-header-control gap |
| D39 | Accepted | A successful TRANSFER generates replacement stay_id and reservation_id upfront and returns them in BOTH the first successful response and the stored idempotency replay result. The authenticated GET /api/v3/reservations/{new_stay_id} is the recovery path after a lost response; the client can also ARRIVE explicitly at the replacement stay using the returned ID. Opposing concurrent transfers serialize via deterministic (facility_id, service_date) lock order | P4; transfer recovery + global deadlock-free |
| D40 | Accepted | Audit EventID includes the operation's idempotency key: `subjectID:action:idemKey` (or `subjectID:action` for legacy callers with empty keys). Two legitimate operations on the same subject with DIFFERENT keys produce two distinct audit events; retries of the SAME key produce no duplicate event or capacity change. Audit uniqueness is preserved (no duplicate-key suppression); audit/outbox writes remain atomic with the mutation | P4; repeatable-but-distinct event audit identities |
| D41 | Superseded | AllocationPolicy.route_required is an opt-in flag; when present and true, every operational commitment (reserve, extend, transfer) MUST reference a verified, currently-valid, unclosed route bound to the destination's safe zone. The store fails closed with ErrRouteRequired BEFORE any capacity change. Missing route_required is treated as not required (preserves existing packages that do not opt in) | P4; explicit policy-driven route omission; SUPERSEDED by D46 |
| D42 | Accepted | The transfer replacement stay is persisted with the VALIDATED replacement route (req.NewRouteID), NEVER the OLD stay's RouteID. The old stay's route bound the origin facility's safe zone; reusing it on the replacement would store a route to the wrong destination. The handler passes newRouteID into StayStore.Transfer | P4; transfer route persistence |
| D43 | Accepted | Snapshot_version is mandatory for commitments that revalidate against the locked snapshot (RESERVE, EXTEND, TRANSFER). Zero or missing values are rejected at the HTTP boundary with 400 INVALID_VALUE, and the inner revalidateSnapshotVersion also returns snapshotStaleError for non-zero expectedVersion if the live version differs. There is no undocumented "legacy path" that bypasses the new contract | P4; snapshot confirmation gating |
| D44 | Accepted | The audit EventID scope is subject:action:actor:idemKey (actor = session_id). Two distinct authorized operator sessions using the SAME idempotency key for distinct corrections do NOT collide merely because subject/action/key match, because actor is part of the identity. Retries by the same actor with the same key remain idempotent at the chain level | P4; corrects D40's actor-blind scope |
| D45 | Accepted | The persisted context resolver and HTTP tests use per-run unique jurisdictions when the test needs the resolver to find its own seeded package; the canonical JTEST jurisdiction is shared across runs and may contain version-2 packages left by earlier runs, so tests must not assume their own seed wins. The resolver itself remains jurisdiction-scoped (D36); the test isolation seam is at the test-fixture layer, not the production code | P4; test isolation under shared DB |
| D46 | Accepted | Route policy authority fail-closed semantics: authoritative allocation_policy must explicitly define route_required. Absent or null route_required fails closed with ErrNoStayPolicy (HTTP 409 VALIDATION_ERROR) across reserve, extend, and transfer, preserving capacity and commitments. Explicit true requires a verified route (ErrRouteRequired / HTTP 409 ROUTE_UNVERIFIED). Explicit false permits omission only if all other gates pass. Supersedes D41's opt-in default | P4; route policy authority |
| D47 | Accepted | Synthetic route boundary isolation: production configuration must reject synthetic evidence (SYNTHETIC_DEMO packages, source artifacts, and routes) for operational commitments (ErrReservationContext / ErrRouteUnavailable). Server-side test/exercise execution is enabled only via explicit dependency injection (WithSyntheticExercise server option), never via request headers, body fields, query parameters, or extension/transfer bypass | P4; synthetic boundary isolation |
| D48 | Accepted | Initial reservation snapshot version check executes under the locked package snapshot acquired by RevalidateReservationContext (FOR UPDATE), ensuring concurrent version bumps commit before version comparison. Missing or non-positive snapshot confirmation rejects with 400 at boundary and snapshotStaleError in transaction. Eliminates window where uncommitted version bump passed stale check before lock acquisition | P4; snapshot ordering |
| D49 | Accepted | Concurrency test evidence observes specific waiter and blocker backend PIDs via pg_stat_activity (wait_event_type = 'Lock') and pg_blocking_pids, proving targeted lock contention between competing transactions rather than global connection count. Negative contention (unrelated advisory locks rejected) and timeouts proven bounded | P4; targeted lock observation |
| D50 | Accepted | Public offline delivery routes (/api/v3/regions/{id}/manifest, /api/v3/packages/{id}/versions/{version}, /api/v3/resources/{id}) strictly partition public from private data. Private bearer tokens, session IDs, citizen identifiers, reservations, and capacity holds are strictly forbidden from delivery response payloads, response headers, and log outputs. Public content is cacheable (Cache-Control: public); private routes retain Cache-Control: no-store | P5; public/private data separation |
| D51 | Accepted | Offline publication storage schema (published_manifests, published_cards, published_resources, migration 0006) decouples public distributions from operational state machines. Auxiliary resources are content-addressed by SHA-256 digest; manifests enforce monotonic revision ordering; card and manifest payloads are validated structurally and cryptographically (Ed25519) | P5; publication persistence & schema 6 |
| D52 | Accepted | Upstream shield cache protects publication sources from client download surges. In-memory caching with singleflight request coalescing (callGroup) bounds concurrent cold-cache misses to a single upstream fetch, satisfying plan/assurance.md §8 upstream quota decoupling | P5; upstream surge shielding |
| D53 | Accepted | Pending offline operations protocol (offlinequeue): durable disk queue persists unconfirmed citizen requests with immutable payload and idempotency key; bearer tokens are never written to disk or logs (resolved via ephemeral in-memory TokenRef). Server revalidates snapshot version and selection expiry window at commit; pending operations never hold server capacity | P5; offline pending queue integrity |
| D54 | Accepted | Constrained network profile verification (400 kbit/s down, 128 kbit/s up, 400ms RTT, 2% loss) is emulated via standard library http.RoundTripper throttling and drop simulation. Transport-level HTTP rate limits and connection drops are simulated; kernel-level packet loss (pfctl/dummynet) and native mobile client runtime remain documented boundaries | P5; network simulation boundaries |
| D55 | Accepted | P5 persists its offline queue as plaintext JSON and its cryptographic keys are synthetic test keys only. Neither is an encryption-at-rest claim or authority-key evidence. The 50 MiB regional-pack number is an illustrative protocol budget until an authorized pack is measured. After restart, absent a process-local monotonic anchor, cached guidance is UNVERIFIABLE until a verified sync rather than restarted as CURRENT | P5; O06 and authority key evidence remain external |
| D57 | Accepted | P6 contract freeze (plan/p6-contract.md) on `CLEAN` defines the typed request/response envelopes for the three new voice endpoints (/voice/transcriptions, /voice/process, /voice/speech), the richer `contracts.ScopedContext` (typed PlaceCandidate/ZoneRef/FacilityRef/RouteRef/EligibleChoice with route↔safe-zone binding and permitted order), the new error codes (TRANSCRIPT_UNAVAILABLE, AUDIO_UNAVAILABLE, MODEL_TIMEOUT, QUEUE_SATURATED, INFERENCE_CANCELLED, TEMPLATE_UNKNOWN, STALE_SNAPSHOT), the SnapshotRevalidator seam for revalidation after slow inference, the private worker protocol (WorkerHealth/ModelInfo/ArtifactDigest/QueueStats/ASR/Middle/TTSWorkerRequest+Response), the typed TTSCacheKey (source_version+template_version+model_revision+voice_revision+settings), and the worker directory ownership boundaries. Existing /api/v3/voice/commands is unchanged. P5 wire formats are unchanged; P6 borrows the publication seam without re-implementing it. Workers consume the freeze and propose amendments; only the coordinator integrates shared wiring | P6; bounded contract stage authorizing six parallel lanes, acceptance remains gated on integrated evidence |
| D58 | Accepted | P6 Multilingual Voice Architecture and Evaluation Seam: Go orchestrator connects private loopback ASR/Middle/TTS workers over typed HTTP contracts; synthetic evaluation harness (backend/eval) passes 20/20 test cases across hi-IN, ml-IN, en-IN. Real inference remains honestly BLOCKED / NOT_EVALUATED pending designated GPU infrastructure, licensed weights, and open decision evidence (O03 language matrix, O11 translation review). Production entrypoints fail closed with HTTP 503 MODEL_UNAVAILABLE when workers are unconfigured; zero consequential writes are guaranteed during voice interactions; direct citizen text/touch navigation operates independently of model worker health | P6 evaluation & integration; preserves O03/O11 boundaries |
| D60 | Accepted | Scoped approved speech, mandatory jurisdiction/version on /voice/speech, post-synthesis revalidation, and inline playable audio delivery: Speech synthesis (/voice/speech) strictly requires explicit jurisdiction and positive source_version, resolving the operational scoped context directly for that jurisdiction rather than falling back to arbitrary active contexts. Version mismatches fail closed with 409 STALE_VERSION. Template arguments are strictly validated against the approved template's ArgSchema with rejection of injection delimiters ({, }). Snapshot validity is revalidated both before synthesis and before audio delivery; source withdrawal during synthesis returns 409 STALE_SNAPSHOT. When TTS synthesis or template translation fails during the full voice pipeline (/voice/process), the validated proposal display actions and rendered text are preserved with audio fallback stage failure rather than collapsing the entire response. Real base64-encoded audio payload is delivered inline in TTSResponse and PipelineAudio | Stage 5; repair speech fallback, approved audio endpoints, and playback contract |
| D61 | Accepted | Authorization & Idempotency Repair (D1 & D2): (1) handleSourceQuarantine strictly denies absent or expired live authorization before replay disclosure or mutation (!has returns errNoAuthorization / 409 Conflict; cross-jurisdiction returns 403 Forbidden), matching sibling handleSourceTransition. (2) reservationPayloadHash implements unambiguous canonical deterministic JSON encoding across all semantically discriminating fields (session_id, facility_id, package_id, route_id, party_size, start_date, end_date, idempotency_key, snapshot_version). Replay with changed party size or snapshot version returns 409 ErrPayloadConflict with capacity/audit untouched; identical replay returns 200 OK. (3) Legacy Hash Compatibility: Existing persisted idempotency rows are preserved (never deleted), while legacy ambiguous hashes are rejected on replay rather than silently accepted. Old rows naturally expire under the existing short-lived TTL (1-24 hours). Identical committed-reservation replay after source withdrawal is preserved | Worker 1; 2026-09-21; fixes confirmed defects D1/D2; regression tests verified on disposable DB |

## Material changes from the previous plan

- Old deterministic-only language parsing becomes constrained language interpretation plus deterministic action/domain validation. Retain the deterministic fallback for simple known commands.
- Old preassigned single-destination demo becomes citizen selection among eligible options; authority policy can still constrain choices.
- Wayanad-only pilot content expands to a proposed multi-state historical exercise matrix. Live coverage is separately authorized.
- Native apps are now launch scope; the web demo remains a migration/reference client.
- Government-only **operational truth** remains mandatory. A licensed public/self-hosted basemap is a possible visual dependency, not a new authority source.
- The Python-phase DONE labels are historical evidence, not proof that new Go phases have passed.
- Model sizes, server counts, performance budgets and third-party licenses are not silently accepted because a plan mentions them.

## Recording later decisions

For a change, record owner/date, problem, chosen option, alternatives, concrete evidence, affected files/contracts, consequences and rollback/revisit trigger. Do not close an open route/source/capacity policy without the responsible authority's evidence. Never recycle an old decision ID to conceal a changed meaning.

## Round-two sequencing decision — 2026-09-21

Accepted explicit user change: prioritize the September 28–29 prototype, actual three-model demo, laptop/phone UI, founder/team understanding and pitch. Preserve production work and architecture; defer broad production completion. This temporarily supersedes D03/frontend sequencing and the Gate-B-before-prototype restriction. It does not waive live-data authority or production acceptance. See [round-two-demo.md](round-two-demo.md).
