# Worker 4 — build the scenario preparation CLI

You are an additional implementation worker, running alongside two existing backend-correction workers. Build the deliverable below, not an audit or a test-only project. Repository: /Users/apple/Documents/Projects/MonitoringZ. Integration target is uppercase CLEAN, never main. Read applicable instructions, GEMINI.md, relevant plan/rules.md, architecture.md, trd.md, open-decisions.md and phases.md, then plan/next-two-worker-1.md and plan/next-two-worker-2.md for ownership boundaries. Inspect current git status/worktrees and existing implementations before editing. Use an isolated new codex/ branch and worktree from current CLEAN; report the base SHA. Do not mutate existing worktrees or branches.

The existing Worker 1 owns shared backend contracts/store/httpserver/orchestration/migrations and final integration. Existing Worker 2 owns inference modules/adapters, eval/loadmodel and existing operational scripts. Own only the paths listed below. Reuse APIs; send an exact required change to Worker 1 instead of editing its files. No root dependency edits without coordination. Do not merge or cherry-pick other workers. Supply ordered new commits to Worker 1 for final integration after its corrections. This task is backend preparation, not P8 frontend work or operational approval.

Preserve unrelated work and all .txt files. No pushes, main changes, resets, rebases, amends, history rewrite, paid infrastructure, model downloads, live government access or deployment to external servers. Do not delete large historical files or raw metrics. Commit coherent verified stages locally. Keep a compact handoff in your owned directory with actual commands/results, limitations and new commit SHAs. Do not claim successful execution without running the relevant path; absent Docker/data/tooling is NOT_RUN, not PASS. Keep verification bounded; no broad audit, load campaign or repeated full suites. Never weaken safety to demonstrate a success.

## Deliverable and ownership

Build `backend/cmd/scenario-prep/` on branch `codex/scenario-preparation-cli`, with documentation/examples under `tools/scenario-prep/`. Use the existing backend Go module and standard library. Own only those two directories. The tool must turn user-supplied local catalogue/package inputs into a consistent, reviewable, deterministic exercise handoff bundle. It must work without a database, model, network or government API. This is a CLI for preparing data, not a frontend, production importer, signing authority or independent schema implementation.

## Read and reuse

Inspect backend/internal/catalogue/catalogue.go, opkg/package.go, httpjson, existing scenario fixtures and source/provenance rules. The catalogue already defines Manifest, Validate, AssessLaunch and gaps; opkg already defines Package, validation and checksum. Reuse these rather than copying validators. Respect Go internal-import boundaries by placing the command inside backend. Existing frontend JSON may be legacy-shaped: do not silently reinterpret it as canonical data. If necessary report its mismatch with a precise migration message; do not edit frontend or invent missing fields.

## Build the workflow

1. A clear `init` operation produces a minimal draft workspace and instructions, not fabricated real state/event data. Keep placeholders only in deliberately invalid drafts labelled DRAFT; never accept placeholders as historical evidence. Do not choose the user's 10–15 states, invent routes/zones or claim model language support. Output to an explicit destination, refusing existing files by default. Help text must explain exactly what the user will supply.

2. A `validate` operation strictly reads a canonical catalogue plus a simple local input index mapping scenario IDs to supplied operational-package JSON paths. Use bounded reads, reject duplicate keys/unknown fields/trailing data using existing parsing facilities. Distinguish structural INVALID from valid-but-INCOMPLETE and from acceptance-ready according to existing validators. Run catalogue structure and launch assessment separately. Reuse opkg validation under an explicitly declared exercise/preparation context; do not disable signature checks and then call data operationally trusted. Report signature verification UNVERIFIED when the tool has no real trusted verifier. No dummy verifier returning true.

3. Cross-check scenario/package mappings: unknown or duplicate scenario IDs, missing referenced files, expected jurisdiction mismatch, conflicting IDs/versions and inconsistent supplied evidence. Do not infer state-to-jurisdiction relationships that the schema cannot establish. Historical event provenance does not authenticate geometry: retain independent provenance labels and do not upgrade synthetic geometry because a real past event is linked. Reject paths escaping the workspace, unsafe symlinks, output-inside-input recursion and unbounded attachments. Never follow URLs automatically or crawl private files.

4. A `report` operation produces concise machine-readable JSON and human-readable Markdown: states supplied, per-state case counts, languages as claimed/evidenced/unknown using existing fields, input errors, missing source references, missing package/zone/route/policy data, signature/approval status and unresolved external gates. Distinguish a local check that a reference is present from verification of the remote source. These are data-preparation reports, not test summaries or completion certificates. Preserve the existing 10–15 states / 2–3 scenarios thresholds; don't invent another acceptance standard.

5. A `bundle` operation copies validated local inputs into a deterministic directory handoff with sorted index, relative paths, content hashes and original provenance. Reuse the opkg checksum for package semantics; label raw-file SHA256 separately. Atomic output publication, refuse overwrite, no secret/private-key inclusion, no DB writes. This handoff is NOT the P5 signed offline-client package: do not create a competing manifest format that claims P5 compatibility, publish it to citizens, activate sources, generate signing keys or add signature placeholders. Structurally invalid inputs cannot produce a ready bundle. Valid incomplete preparation bundles may be allowed only with explicit draft status and missing-items report, never masquerading as launch-ready.

6. Provide one small clearly synthetic example using existing repository fixtures where safe, with honest exercise clock/provenance. Do not fabricate named historical disasters or pretend the example satisfies O01. Examples must demonstrate the real CLI workflow from supplied inputs to report/bundle, including how the user replaces draft files with real evidence later. Keep deterministic injected exercise time where existing validation needs it; don't move fixture expiry dates just to get green output.

## Finish and coordinate

Provide helpful exit codes and errors distinguishing invalid, incomplete, I/O failure and success; document them for automation. Do not introduce a general plugin system, new database, network service, UI or library dependency. Build/run the actual command on its small example and one invalid input, inspect resulting bundle and deterministic repeat output; keep only a small necessary regression check for nontrivial path/parsing behavior. This is a build assignment, not an exhaustive test campaign.

Do not modify catalogue/opkg/shared contracts or root module metadata concurrently with Worker 1. If an API gap exists, report the exact needed change and complete the unaffected workflow; don't duplicate validation to evade ownership. Supply `tools/scenario-prep/HANDOFF.md`, new commit SHAs, usage and actual execution limits. Do not close O01, language approval, route/stay policy or any phase gate. Worker 1 owns final integration and shared docs.
