# Worker 3 — build the Go backend deployment package

You are an additional implementation worker, running alongside two existing backend-correction workers. Build the deliverable below, not an audit or a test-only project. Repository: /Users/apple/Documents/Projects/MonitoringZ. Integration target is uppercase CLEAN, never main. Read applicable instructions, GEMINI.md, relevant plan/rules.md, architecture.md, trd.md, open-decisions.md and phases.md, then plan/next-two-worker-1.md and plan/next-two-worker-2.md for ownership boundaries. Inspect current git status/worktrees and existing implementations before editing. Use an isolated new codex/ branch and worktree from current CLEAN; report the base SHA. Do not mutate existing worktrees or branches.

The existing Worker 1 owns shared backend contracts/store/httpserver/orchestration/migrations and final integration. Existing Worker 2 owns inference modules/adapters, eval/loadmodel and existing operational scripts. Own only the paths listed below. Reuse APIs; send an exact required change to Worker 1 instead of editing its files. No root dependency edits without coordination. Do not merge or cherry-pick other workers. Supply ordered new commits to Worker 1 for final integration after its corrections. This task is backend preparation, not P8 frontend work or operational approval.

Preserve unrelated work and all .txt files. No pushes, main changes, resets, rebases, amends, history rewrite, paid infrastructure, model downloads, live government access or deployment to external servers. Do not delete large historical files or raw metrics. Commit coherent verified stages locally. Keep a compact handoff in your owned directory with actual commands/results, limitations and new commit SHAs. Do not claim successful execution without running the relevant path; absent Docker/data/tooling is NOT_RUN, not PASS. Keep verification bounded; no broad audit, load campaign or repeated full suites. Never weaken safety to demonstrate a success.

## Deliverable and ownership

Build a reproducible local/staging deployment package under `deploy/go-backend/` only, on branch `codex/go-deployment-package`. Include the container build, Compose configuration, explicit migration operation, useful lifecycle commands and concise operator instructions. Existing deploy/docker-compose.demo.yml launches the Python application; leave it and the root Dockerfile untouched. Reuse patterns from backend/deploy/recovery where appropriate, but do not edit them. This is a practical single-host deployment package, not Kubernetes, Terraform, a new platform or an HA claim.

## Build in this order

1. Inspect backend/go.mod, go.sum, cmd/sthira/main.go, health routes, migrations and recovery deployment. Create a multi-stage Go image from the pinned actual toolchain, minimal runtime, non-root user and working TLS certificates/timezone support if required. Preserve actual signal/shutdown behavior. Do not bundle source credentials, datasets, model weights or raw test logs. Keep build context small through an owned Dockerfile-specific ignore file if supported; otherwise use an explicit safe context strategy without modifying root ignore files. Record image/tool versions; do not invent image digests or promise bit-identical builds without proof.

2. Compose the actual Go API and version-compatible PostgreSQL/PostGIS with durable named storage, private networking and bounded resources. Expose only the API on loopback by default; do not publish PostgreSQL or model-worker ports. Match existing configuration names, actual health endpoints and schema readiness. A healthy database is not operationally approved guidance; absent sources/IdP/models must remain fail-closed. Never seed authorizations, bypass a verifier, enable synthetic exercise through environment guessing, or change the API to make the demo green.

3. Add an explicit one-shot migration command using existing migration files and PostgreSQL tooling. Do not automatically race migrations on every API replica startup. Stop on errors; identify applied revision and lock migration execution. Respect existing files' transaction/revision behavior; do not implement a second incompatible schema history. Apply only unapplied supported migrations and reject inconsistent history. Ask Worker 1 for any migration-contract ambiguity while completing other files. Do not implement automatic destructive down migrations.

4. Provide small owned commands for build, start, status, logs and stop. Validate required configuration before starting resources; do not silently use fixed passwords. Use safe permissions and ignored/local secret input outside tracked files, no credentials in command echoes/handoffs. Follow the current backend's actual secret interface; if it requires an environment DSN, document that limitation honestly instead of inventing *_FILE support. Commands must be scoped to a unique explicit project name; never stop unrelated processes or remove volumes on routine shutdown. Keep persistent data on rebuild/update.

5. Include explicit local backup/restore operations reusing PostgreSQL native tooling and established runbook semantics. Backups must not land in tracked repository paths, expose passwords, or silently overwrite files. Restore only into a newly created isolated named target, never the active data store by default. No auto-deletion of backups. Explain that periodic scheduling, PITR/HA and real production retention remain deployment-owner decisions; do not build an unrequested backup platform.

6. Integrate existing worker URL settings as optional external service endpoints. Do NOT build ASR/TTS/vLLM images or guess their changing dependency/entrypoint contracts; Worker 2 owns those. Clearly report voice unavailable when no workers are configured. Supply an extension point using existing configuration, not a fake inference service. No cloud purchasing, public TLS/domain assumptions, fabricated hardware sizing or fixed million-user capacity claim.

## Done means a usable package

Run configuration rendering and image build if Docker is available. Start only an owned local stack, explicitly migrate an empty owned DB, exercise actual API health and persistence across API restart, and demonstrate the explicit backup/isolated-restore command on small owned data if supported. Stop only your stack; preserve its data unless you explicitly created disposable verification resources. Do not turn this into a large testing assignment: these are bounded checks of what you built. If runtime unavailable, verify static config and script syntax, label runtime NOT_RUN and still deliver complete executable artifacts.

Document exact commands, required inputs, ports, first startup, migration/update order, rollback limitations, logs, secret handling and cleanup. Keep deployment configuration aligned with real APIs. Report any interface mismatch to Worker 1 and avoid claiming production readiness. Deliver new commits plus `deploy/go-backend/HANDOFF.md`; do not edit global phase ledgers. Worker 1 integrates this after its code corrections.
