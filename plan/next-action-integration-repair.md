# Single worker: integrate A/B and repair demonstrated failures

Execute this prompt in one new chat. This is P5/P6/P7 correction and integration, not permission to start P8. Do not merely produce another plan. Another broad test count is not acceptance; the actual public/worker/database boundaries below must work.

## Start and preserve

Repository `/Users/apple/Documents/Projects/MonitoringZ`, integration target uppercase `CLEAN`, never main. Read applicable instructions and GEMINI.md; read the product/rules/architecture/decisions/open-decisions and phase gates, then `plan/reviews/review-e0babda-3e6daac.md`, `plan/next-action-worker-a.md`, `plan/next-action-worker-b.md` and both branch handoffs. Read source and callers selectively, never raw metrics or model weights.

Review baseline: CLEAN de0b4e1 before the latest review-doc commit; Worker A e0babda on codex/p567-backend-corrections, Worker B 3e6daac on codex/p567-inference-corrections. Recheck actual HEAD/status/worktrees before proceeding. Both worker branches forked b0b9772. B has unrelated uncommitted security-checks.json/.md changes; preserve those. No .txt changes, push, main merge, reset, amend, history rewrite, shared DB truncation or deletion of another worker's artifacts.

Create an isolated integration worktree on `codex/p567-integration-repair` from current CLEAN. Cherry-pick actual lane commits in order: A 2747ca5, 0ce0171, d1dc5a6, aee4df9, e0babda; B 5e98cf5, f3c747b, 40a95c8, 7e2f0ad, 3e6daac. Confirm ancestry/contents before applying; omit already integrated changes. Do not replace CLEAN with either lane tree, which lacks the review/prompt commit. Integration of these task commits is in scope; do not absorb unrelated edits. Keep a compact acceptance table in plan/integration-repair-handoff.md and commit coherent verified fixes. Imported commits have known defects; do not call the initial combined tree verified.

Preserve approved model selection: IndicConformer-600M-Multi, Sarvam-30B with 2.4B active non-embedding parameters, Indic Parler-TTS. No model download, paid inference or GPU rental without existing explicit authorization. Government data, route/stay policy, translation approval and operator identity remain authoritative external gates. A missing model implementation is internal unfinished work, not a hardware blocker.

## Stage 1 — restore the actual public pipeline and decoder safety

First reproduce `go test -count=1 -run TestVoiceProcess_HandleProcessHappyPath -v ./internal/httpserver` from backend. It currently fails 400 UNKNOWN_FIELD jurisdiction because Process decodes PipelineRequest bytes as ModelOutput. Fix at the responsible boundary: strict PipelineRequest handling for citizen bytes, strict model schema handling for model bytes. Do not remove strict validation or pass nil raw bytes merely to bypass it. Keep distinct public audio/request and model-response limits. Enforce required/forbidden model-field presence (including explicit zero/empty forbidden fields), correlations, enums and tagged variants before loss through typed decoding; preserve the repaired nonempty-extra-field guards and stale-after-failed-TTS behavior.

Keep a real public HTTP happy-path regression and failure checks. Run those with ProductionValidator and actual server handlers, using fake inference only where appropriate. Unit tests sending a model-shaped raw body beside a PipelineRequest cannot establish success.

Reproduce malformed WAV fmt chunk size 0xffffffff in a 44-byte RIFF/WAVE input: decodeRIFFWAV currently panics. Bound every chunk slice/advance with overflow-safe checks. Unknown-length data from the controlled ffmpeg output must not authorize unknown-length fmt or other chunks. Keep actual Ogg/WebM success and ordinary truncated WAV rejection. Add a public decoder/handler malformed-input no-panic regression plus a bounded fuzz corpus for chunk sizes. Do not merely recover a panic and ignore malformed data.

## Stage 2 — finish publication and context authority, then test real DB

Do not add another observer wrapper. Trace actual production lifecycle and connect trusted publication/promotion/source transitions to persisted authority checks and delivery. NewPublisher/WithObserver currently have no production caller, and an in-memory fan-out bus is not cross-process invalidation. Reuse existing trusted operator/session/grant boundaries; missing verifier stays fail-closed. Preserve one-transaction validation/write.

Before promotion, transactionally prove attributed source/package/jurisdiction/current authorization/signature/expiry/supersession and manifest-card version relationships. Legacy unattributed rows cannot promote. Stage newer publications without hiding the current one; ensure one coherent current selection and immutable identical retry versus conflict. Restrict unchecked Store paths from application access. Fix consistent source/package lock order and demonstrate controlled contention using backend PID/lock evidence, not just two unsynchronized goroutines.

Test trusted lifecycle -> signed persisted publication -> actual cached HTTP delivery -> source withdrawal/quarantine/supersession -> subsequent denial/signed revocation. Exercise independent instances under a declared consistency bound. Do not manually invoke cache invalidation in the acceptance test. Handle in-flight reads and honest immutable-card validity semantics.

Repair A3 fixtures against actual migrations: they currently insert nonexistent `artifacts` and reuse MNF-LEGACY-1 across packages. Use unique identities and existing seed helpers. Tests must fail on supplied-DSN connection errors. Replace the legacy test that only logs a gap with an actual promotion attempt and assertions on unchanged state. Run all A3 tests on a uniquely owned migrated PostgreSQL/PostGIS DB; record actual commands/results. Never skip them and mark PASS.

Complete template binding to current jurisdiction/source/version and approval lifecycle, and resolve spoken names/counts/IDs from authoritative context. Synthetic marking is useful but not the entire approval implementation. Do not invent approved translations; implement/test the boundary with isolated fixtures and keep missing approval external. Remove one-day/party-size-zero suitability assumptions: zero free inventory currently passes the zero-sized-party comparison. Represent unknown eligibility honestly; require accepted inputs for suitability. Do not label alphabetic IDs with policy PermittedRank unless an accepted ordering establishes it. Preserve valid destination browsing separately from a capacity promise.

## Stage 3 — replace false-ready model stubs with real integration

ASR currently constructs ONNX sessions without running them and returns `[unverified:real-inference-stub]`. Remove that false transcript. Establish the actual selected model's pinned local-artifact loader, preprocessing, language-conditioned inference and decoding from installed source or primary documentation. No guessed ONNX layout/decoder or fallback invented transcript. TTS must use the supported Indic Parler-TTS model/tokenizer/description/generation/audio API, not an assumed generic AutoModel.generate(text, language, voice) returning .wav. Pin justified compatible dependencies and exact artifact metadata; loading must stay local-only.

READY must follow successful load/required warm-up, not file presence. The review reproduced READY with dummy `{}` files for both adapters. Test malformed/missing files, import/load failure and valid fake-model initialization through the actual Python entry points. Advertise only verified loaded capabilities; filesystem presence cannot resolve O03/O11. Test protocol with injected fake dependencies and separately provide opt-in real-artifact inference checks, clearly NOT_RUN if unavailable. If APIs cannot be established, leave explicitly blocked without placeholder behavior and record precise unfinished engineering.

Fix IPC boundedness at its owner: cancellation/deadline must cover admission AND stdin writes, not only response waiting. A child that stops reading must not hang request cancellation or Close; terminate/reap/reset uncertain streams. Register startup waiter before launching the reader/probe race. Synchronize demux error state; reject malformed/duplicate/mismatched protocol data consistently; clean up readers/children. Keep existing correlation improvements. Add actual subprocess tests for stopped reads with a full pipe, startup-response race, cancel/retry, malformed response, process exit and shutdown under `-race`, with deterministic deadlines. Near-limit TTS audio must still succeed.

Remove the unverified custom Sarvam template from the active path. Use the pinned model's actual tokenizer template and supported vLLM reasoning/structured-output settings, with outbound-path assertions tied to evidence. A Jinja variable never used by the template is not reasoning control, and wrong tokens may not generate an HTTP error. Record exact runtime/model revisions and hardware prerequisites; no claimed accuracy/throughput without real measurement.

## Stage 4 — verify cross-module protocol and evaluation using actual handlers

Middle evaluation currently sends unsupported source_version/data_version_hint and decodes a standard data envelope as a flat object, yielding empty status with nil error. TTS mixes public speech and private synthesis and defaults missing versions to 1. Fix endpoint-specific protocols using current shared contracts, without widening public schema to fit the harness or inventing values. Validate required envelope/correlation/content, fail malformed success responses, and distinguish unavailable/refused outcomes from quality successes.

Run real ASR/middle/TTS server constructors with fake inference runtimes through HTTPWorkerClient and public handler; run the eval provider against that actual host. No hand-written replacement server copying the provider's invented fields. Cover valid response/audio, unsupported language, stale source, bad ID/envelope, cancellation and unhealthy->recovered readiness. Fake inference verifies plumbing, not language quality.

## Stage 5 — bounded P7 evidence and final acceptance

Smoke script still has an uninitialized STHIRA_PID exit trap and second-resolution resource names; add ownership tracking and safe bounded cleanup, including failure before startup. Never drop an existing DB or kill a process you did not create. Seed an actual isolated successful business case and assert semantic response fields; a nonempty error/empty-candidate envelope is not success. Make every required check fail the run, including malformed 200/empty candidates and readiness failure. Preserve status-0 rejection. Use a small smoke, not millions of raw metrics.

Make security results distinguish required NOT_RUN coverage from a clean audit; configure Trivy findings exit behavior and reproducibly identify/pin tools. Test failure propagation with deterministic fake tool executables, distinguishing tool crash and findings. Run available real checks, record missing tools honestly, and keep evidence in Markdown/JSON. Preserve existing .txt evidence. Revalidate SPDX metadata without inventing package/license information.

Finally run formatting/vet/build and all tests in backend and five separate modules, actual DB suites with current migrations, affected Python tests, actual-handler conformance, bounded smoke and IPC race tests. Enable both crashtest build tag and STHIRA_RUN_PROCESS_TESTS=1 for the two process crash/retry tests. Required tests skipped/uncompiled are not PASS. Preserve failure details and exit statuses. No unbounded repeats of already green checks without changes.

Update prompt/decisions/open-decisions and active phase ledger from verified evidence; retain history and external gates. Record exact SHAs, implemented versus NOT_RUN items, and remaining defects. Keep P5/P6/P7 acceptance open where required evidence is missing and gate B NOT_READY; stop before P8.

Leave combined changes committed on the integration branch. Report its SHA and whether CLEAN can fast-forward; do not push or move main. Preserve CLEAN/unrelated work until integration is explicitly applied. A final handoff must not call these five stages complete if any mandatory behavior is still a placeholder, test bypass, absent caller or mislabeled external blocker.
